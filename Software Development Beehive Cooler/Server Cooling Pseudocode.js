// Create a variable that takes the string value of the input from temperature and humidity sensor one
// Split the two values into two numerical variables

//If temperature is greater than "blank"  then send an output signal to pin to activate fan.
//If humidity is greather than "blank", also activate the fan
// If both humidity and temperature are at certain (lower) values, activate fan

//Deactivation "else" statements once temperature and humidity lower

//Shell Script (Bash) code to activate pins of fans:
// echo "16" > /sys/class/gpio/export
// echo "out" > /sys/class/gpio/gpio16/direction
// echo "high" > /sys/class/gpio/gpio16/direction  to activate
// echo "low" > /sys/class/gpio/gpio16/direction  to deactivate

//Reading command from temperature humidity sensor requirements
// import Adafruit_DHT
// import datetime
// import RPi.GPIO as GPIO (Abbreviates in program)
// sensor = Adafruit_DHT.AM2302
// pin = 4 (connects to a GPIO pin)

//Running the reading temp/humidity reading program
// sudo ./simpletest
//converting celsius into fahrenheait: temperature * 9/5 + 32


// At the beginning type :
//!/usr/bin.python














